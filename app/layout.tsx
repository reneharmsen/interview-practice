import './ui/globals.css';

// import { Cart } from '@/components/cart';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {}
        {children}
        </body>
    </html>
  );
}