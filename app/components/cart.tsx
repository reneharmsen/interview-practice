'use client'

import { useState, useEffect } from 'react';
import { getProductData } from '@/lib/data';

export default function Cart() {

    return (
        <div>0 products in cart</div>
    )
}